node_modules
# Keep environment variables out of version control
.env

# Ignore JavaScript files in src/ (if compiled in-place)
src/**/*.js
src/**/*.js.map
DS_Store
# Ignore the dist directory if TypeScript compiles to a different directory
dist/

